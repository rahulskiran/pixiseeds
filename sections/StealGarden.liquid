{%- liquid
  assign pt = section.settings.padding_top
  assign pb = section.settings.padding_bottom

  assign section_animation = section.settings.animations | default: 'inherit'
  assign animation_effect = settings.animations
  if section_animation != 'inherit'
    assign animation_effect = section_animation
  endif
  assign animated = false
  if animation_effect != 'none'
    assign animated = true
  endif
  if request.design_mode
    assign animated = false
  endif

  assign image_aspect_ratio = section.settings.image_aspect_ratio
  if section.settings.image != blank and image_aspect_ratio == 'original'
    assign image_aspect_ratio = section.settings.image.aspect_ratio
  endif
  if section.settings.image == blank and image_aspect_ratio == 'original'
    assign image_aspect_ratio = '16/9'
  endif

  assign heading_tag = section.settings.heading_tag | default: 'h2'

  assign hs = section.settings.hotspot_style
  if hs == 'dark'
    assign sg_def_core = '#1a1a1a'
    assign sg_def_core_border = 'rgba(255, 255, 255, 0.2)'
    assign sg_def_ring = 'rgba(255, 226, 3, 1)'
    assign sg_def_ring_outline = 'rgba(0, 0, 0, 0.2)'
    assign sg_def_arrow = '#ffffff'
  else
    assign sg_def_core = '#ffffff'
    assign sg_def_core_border = 'rgba(255, 255, 255, 0.5)'
    assign sg_def_ring = 'rgba(255, 255, 255, 0.5)'
    assign sg_def_ring_outline = 'rgba(255, 255, 255, 0.4)'
    assign sg_def_arrow = '#ffffff'
  endif

  assign sg_core = sg_def_core
  if section.settings.hotspot_fill_color != blank
    assign sg_core = section.settings.hotspot_fill_color
  endif
  assign sg_core_border = sg_def_core_border
  if section.settings.hotspot_border_color != blank
    assign sg_core_border = section.settings.hotspot_border_color
  endif
  assign sg_ring = sg_def_ring
  if section.settings.hotspot_pulse_color != blank
    assign sg_ring = section.settings.hotspot_pulse_color
  endif
  assign sg_ring_outline = sg_def_ring_outline
  if section.settings.hotspot_pulse_border_color != blank
    assign sg_ring_outline = section.settings.hotspot_pulse_border_color
  endif
  assign sg_arrow = sg_def_arrow
  if section.settings.hotspot_arrow_color != blank
    assign sg_arrow = section.settings.hotspot_arrow_color
  endif
-%}

{% style %}
  .m-stealgarden--{{ section.id }} {
    --m-sg-radius: {{ section.settings.image_radius }}px;
    --m-sg-header-gap: {{ section.settings.header_gap }}px;
    --m-sg-title-size-m: {{ section.settings.title_size_mobile }}px;
    --m-sg-title-size-d: {{ section.settings.title_size_desktop }}px;
    --m-sg-sub-size: {{ section.settings.subtitle_size }}px;
    --m-sg-pin-size: {{ section.settings.hotspot_size }}px;
    --m-sg-hotspot-core: {{ sg_core }};
    --m-sg-hotspot-core-border: {{ sg_core_border }};
    --m-sg-hotspot-ring: {{ sg_ring }};
    --m-sg-hotspot-ring-outline: {{ sg_ring_outline }};
    --m-sg-hotspot-arrow: {{ sg_arrow }};
  }
  #shopify-section-{{ section.id }} .m-stealgarden.m-section--padding {
    padding-top: {{ section.settings.padding_top_mobile }}px;
    padding-bottom: {{ section.settings.padding_bottom_mobile }}px;
  }
  @media screen and (min-width: 750px) {
    #shopify-section-{{ section.id }} .m-stealgarden.m-section--padding {
      padding-top: {{ pt }}px;
      padding-bottom: {{ pb }}px;
    }
  }
{% endstyle %}

<section
  class="m-section m-stealgarden m-stealgarden--{{ section.id }} m-section--padding m-color-{{ section.settings.color_scheme }} m-gradient"
  data-section-type="steal-garden"
  data-section-id="{{ section.id }}"
  style="--section-padding-top: {{ pt }}px; --section-padding-bottom: {{ pb }}px;"
>
  <div class="m-page-width">
    <header class="m-stealgarden__header m:text-center{% if animated %} m-scroll-trigger animate--{{ animation_effect }}{% endif %}">
      {% if section.settings.heading != blank %}
        <{{ heading_tag }} class="m-stealgarden__title">{{ section.settings.heading | escape }}</{{ heading_tag }}>
      {% endif %}
      {% if section.settings.subtitle != blank %}
        <p class="m-stealgarden__subtitle">{{ section.settings.subtitle | escape }}</p>
      {% endif %}
    </header>

    <div class="m-stealgarden__stage">
      <div
        class="m-stealgarden__canvas{% if animated %} m-scroll-trigger animate--{{ animation_effect }}{% endif %}"
      >
        {% if section.settings.image != blank %}
          {%- liquid
            assign sg_img = section.settings.image
            assign image_height = sg_img.width | divided_by: sg_img.aspect_ratio
            assign sg_alt = sg_img.alt | default: section.settings.heading | default: shop.name | escape
          -%}
          <div class="m-stealgarden__media m-image m-image--rounded" style="--aspect-ratio: {{ image_aspect_ratio }};">
            {{
              sg_img
              | image_url: width: 3840
              | image_tag:
                loading: 'lazy',
                class: 'm-stealgarden__img',
                width: sg_img.width,
                height: image_height,
                sizes: '(min-width: 1200px) min(1200px, 90vw), 100vw',
                widths: '375, 550, 750, 1100, 1500, 1780, 2000, 3000, 3840',
                alt: sg_alt
            }}
          </div>
        {% else %}
          <div class="m-stealgarden__media m-stealgarden__media--placeholder m-image m-image--rounded m-image--placeholder" style="--aspect-ratio: {{ image_aspect_ratio }};">
            {{ 'lifestyle-1' | placeholder_svg_tag: 'm-placeholder-svg m:w-full m:h-full' }}
          </div>
        {% endif %}

        {% for block in section.blocks %}
          {% if block.type != 'hotspot' %}
            {% continue %}
          {% endif %}
          {% assign sg_pin_i = forloop.index0 %}
          {% assign prod = blank %}
          {% if block.settings.product != blank %}
            {% assign prod = all_products[block.settings.product] %}
          {% endif %}
          {% unless request.design_mode %}
            {% if prod == blank %}
              {% continue %}
            {% endif %}
          {% endunless %}
          {% liquid
            assign top = block.settings.offset_top | default: 50
            assign left = block.settings.offset_left | default: 50
            assign y_pos = 'm-stealgarden__pop--top'
            if top > 50
              assign y_pos = 'm-stealgarden__pop--bottom'
            endif
          %}
          {% if prod != blank %}
            <a
              href="{{ prod.url }}"
              class="m-stealgarden__pin m-stealgarden__pin--{{ section.settings.hotspot_style }} m-stealgarden__pin--anim-{{ section.settings.hotspot_dot_animation }}"
              style="top: {{ top }}%; left: {{ left }}%; --m-sg-pin-stagger: {{ sg_pin_i | times: 120 }}ms;"
              aria-label="{{ prod.title | escape }}"
              {{ block.shopify_attributes }}
            >
          {% else %}
            <div
              class="m-stealgarden__pin m-stealgarden__pin--no-product m-stealgarden__pin--{{ section.settings.hotspot_style }} m-stealgarden__pin--anim-{{ section.settings.hotspot_dot_animation }}"
              style="top: {{ top }}%; left: {{ left }}%; --m-sg-pin-stagger: {{ sg_pin_i | times: 120 }}ms;"
              role="img"
              aria-label="Product dot — choose a product in the sidebar"
              {{ block.shopify_attributes }}
            >
          {% endif %}
          {% if section.settings.show_hotspot_arrow %}
            <span class="m-stealgarden__pin-arrow" aria-hidden="true">
              <svg width="14" height="14" viewBox="0 0 14 14" fill="none" xmlns="http://www.w3.org/2000/svg" focusable="false">
                <path d="M7 1v10M7 11l3.5-3.5M7 11L3.5 7.5" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
              </svg>
            </span>
          {% endif %}
          <span class="m-stealgarden__pin-ring" aria-hidden="true"></span>
          <span class="m-stealgarden__pin-core" aria-hidden="true"></span>
          {% if section.settings.show_preview and prod != blank %}
            <span class="m-stealgarden__pop {{ y_pos }}" aria-hidden="true">
              <span class="m-stealgarden__pop-inner">
                {% if prod.featured_image != blank %}
                  <span class="m-stealgarden__pop-img">
                    {{
                      prod.featured_image
                      | image_url: width: 200
                      | image_tag:
                        loading: 'lazy',
                        widths: '100, 200',
                        sizes: '100px',
                        alt: prod.featured_image.alt | default: prod.title | escape
                    }}
                  </span>
                {% endif %}
                <span class="m-stealgarden__pop-title">{{ prod.title | escape }}</span>
                <span class="m-stealgarden__pop-price">{{ prod.price | money }}</span>
              </span>
            </span>
          {% elsif prod == blank and request.design_mode %}
            <span class="m-stealgarden__pop m-stealgarden__pop--hint {{ y_pos }}" aria-hidden="true">
              <span class="m-stealgarden__pop-inner m-stealgarden__pop-inner--hint"> Select a product · use Vertical &amp; Horizontal sliders to move this dot </span>
            </span>
          {% endif %}
          {% if prod != blank %}
            </a>
          {% else %}
            </div>
          {% endif %}
        {% endfor %}
      </div>
    </div>
  </div>
</section>

{% style %}
  .m-stealgarden__header {
    margin-bottom: var(--m-sg-header-gap);
    max-width: 48rem;
    margin-left: auto;
    margin-right: auto;
  }
  .m-stealgarden__title {
    font-family: var(--font-stack-header);
    font-size: var(--m-sg-title-size-m);
    line-height: 1.2;
    font-weight: var(--font-weight-header);
    margin: 0;
    color: rgb(var(--color-foreground));
  }
  @media screen and (min-width: 750px) {
    .m-stealgarden__title {
      font-size: var(--m-sg-title-size-d);
    }
  }
  .m-stealgarden__subtitle {
    font-family: var(--font-stack-body);
    font-size: var(--m-sg-sub-size);
    line-height: 1.5;
    margin: 0.75rem 0 0;
    color: rgba(var(--color-foreground), 0.75);
  }
  .m-stealgarden__stage {
    width: 100%;
  }
  .m-stealgarden__canvas {
    position: relative;
    width: 100%;
    border-radius: var(--m-sg-radius);
    overflow: hidden;
  }
  .m-stealgarden__media {
    border-radius: var(--m-sg-radius);
  }
  .m-stealgarden__media.m-image--rounded {
    --image-corner-radius: var(--m-sg-radius);
  }
  .m-stealgarden__img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    vertical-align: middle;
    display: block;
  }
  .m-stealgarden__pin {
    position: absolute;
    z-index: 2;
    width: var(--m-sg-pin-size);
    height: var(--m-sg-pin-size);
    margin: 0;
    padding: 0;
    border: 0;
    background: transparent;
    cursor: pointer;
    transform: translate(-50%, -50%);
    text-decoration: none;
    color: inherit;
    -webkit-tap-highlight-color: transparent;
    touch-action: manipulation;
  }
  .m-stealgarden__pin:focus-visible {
    outline: 2px solid rgb(var(--color-foreground));
    outline-offset: 4px;
    border-radius: 50%;
  }
  .m-stealgarden__pin--no-product {
    cursor: default;
  }
  .m-stealgarden__pin--no-product .m-stealgarden__pin-core {
    opacity: 0.9;
    background: rgba(255, 180, 80, 0.95);
    box-shadow: 0 0 0 3px rgba(255, 255, 255, 0.45), 0 2px 12px rgba(0, 0, 0, 0.25);
  }
  .m-stealgarden__pop-inner--hint {
    font-size: 12px;
    line-height: 1.35;
    font-weight: 500;
    color: rgba(var(--text-black), 0.85);
  }
  @media (hover: hover) and (pointer: fine) {
    .m-stealgarden__pin--no-product:hover .m-stealgarden__pop--hint {
      opacity: 1;
      visibility: visible;
    }
    .m-stealgarden__pin--no-product:hover .m-stealgarden__pop--hint.m-stealgarden__pop--top {
      transform: translate(-50%, 0);
    }
    .m-stealgarden__pin--no-product:hover .m-stealgarden__pop--hint.m-stealgarden__pop--bottom {
      transform: translate(-50%, 0);
    }
  }
  .m-stealgarden__pin-ring {
    position: absolute;
    inset: 0;
    margin: auto;
    width: 100%;
    height: 100%;
    border-radius: 50%;
    background: var(--m-sg-hotspot-ring);
    box-shadow: 0 0 0 2px var(--m-sg-hotspot-ring-outline);
    opacity: 0.5;
    animation-delay: var(--m-sg-pin-stagger, 0ms);
  }
  .m-stealgarden__pin--anim-pulse .m-stealgarden__pin-ring {
    animation: m-stealgarden-pulse 2s cubic-bezier(0, 0, 0.2, 1) infinite;
    animation-delay: var(--m-sg-pin-stagger, 0ms);
  }
  .m-stealgarden__pin--anim-bounce .m-stealgarden__pin-ring {
    animation: m-stealgarden-bounce 1.6s cubic-bezier(0.45, 0, 0.2, 1) infinite;
    animation-delay: var(--m-sg-pin-stagger, 0ms);
  }
  .m-stealgarden__pin--anim-breathe .m-stealgarden__pin-ring {
    animation: m-stealgarden-breathe 2.4s ease-in-out infinite;
    animation-delay: var(--m-sg-pin-stagger, 0ms);
  }
  .m-stealgarden__pin--anim-none .m-stealgarden__pin-ring {
    animation: none;
    opacity: 0.4;
  }
  .m-stealgarden__pin-core {
    position: absolute;
    inset: 0;
    margin: auto;
    width: 38%;
    height: 38%;
    min-width: 10px;
    min-height: 10px;
    max-width: 18px;
    max-height: 18px;
    border-radius: 50%;
    background: var(--m-sg-hotspot-core);
    box-shadow: 0 0 0 3px var(--m-sg-hotspot-core-border), 0 2px 12px rgba(0, 0, 0, 0.28);
    animation-delay: var(--m-sg-pin-stagger, 0ms);
  }
  .m-stealgarden__pin--anim-bounce .m-stealgarden__pin-core {
    animation: m-stealgarden-core-bounce 1.6s cubic-bezier(0.45, 0, 0.2, 1) infinite;
    animation-delay: var(--m-sg-pin-stagger, 0ms);
  }
  .m-stealgarden__pin--anim-breathe .m-stealgarden__pin-core {
    animation: m-stealgarden-core-breathe 2.4s ease-in-out infinite;
    animation-delay: var(--m-sg-pin-stagger, 0ms);
  }
  .m-stealgarden__pin-arrow {
    position: absolute;
    left: 50%;
    bottom: calc(50% + (var(--m-sg-pin-size) * 0.4));
    transform: translateX(-50%);
    display: flex;
    align-items: center;
    justify-content: center;
    width: 18px;
    height: 18px;
    color: var(--m-sg-hotspot-arrow);
    pointer-events: none;
    filter: drop-shadow(0 1px 2px rgba(0, 0, 0, 0.4));
    animation: m-stealgarden-arrow-nudge 1.85s ease-in-out infinite;
    animation-delay: var(--m-sg-pin-stagger, 0ms);
  }
  @media (prefers-reduced-motion: reduce) {
    .m-stealgarden__pin-ring,
    .m-stealgarden__pin-core,
    .m-stealgarden__pin-arrow {
      animation: none !important;
    }
    .m-stealgarden__pin--anim-none .m-stealgarden__pin-ring {
      opacity: 0.4;
    }
  }
  .m-stealgarden__pin-arrow svg {
    display: block;
    width: 14px;
    height: 14px;
  }
  @keyframes m-stealgarden-pulse {
    0%,
    100% {
      transform: scale(1);
      opacity: 0.5;
    }
    50% {
      transform: scale(1.14);
      opacity: 0.22;
    }
  }
  @keyframes m-stealgarden-bounce {
    0%,
    100% {
      transform: scale(1);
      opacity: 0.48;
    }
    40% {
      transform: scale(1.08);
      opacity: 0.35;
    }
    55% {
      transform: scale(1.2);
      opacity: 0.18;
    }
  }
  @keyframes m-stealgarden-breathe {
    0%,
    100% {
      transform: scale(1);
      opacity: 0.42;
    }
    50% {
      transform: scale(1.1);
      opacity: 0.55;
    }
  }
  @keyframes m-stealgarden-core-bounce {
    0%,
    100% {
      transform: scale(1);
    }
    45% {
      transform: scale(1.06);
    }
    70% {
      transform: scale(0.96);
    }
  }
  @keyframes m-stealgarden-core-breathe {
    0%,
    100% {
      transform: scale(1);
    }
    50% {
      transform: scale(1.08);
    }
  }
  @keyframes m-stealgarden-arrow-nudge {
    0%,
    100% {
      transform: translateX(-50%) translateY(0);
      opacity: 1;
    }
    50% {
      transform: translateX(-50%) translateY(5px);
      opacity: 0.82;
    }
  }
  .m-stealgarden__pop {
    position: absolute;
    pointer-events: none;
    opacity: 0;
    visibility: hidden;
    transition:
      opacity 0.25s ease,
      visibility 0.25s ease,
      transform 0.25s ease;
    z-index: 5;
    width: min(200px, 70vw);
  }
  .m-stealgarden__pop-inner {
    display: flex;
    flex-direction: column;
    gap: 4px;
    padding: 10px 12px;
    background: rgba(var(--bg-white), 1);
    color: rgba(var(--text-black), 1);
    border-radius: var(--corner-radius, 6px);
    box-shadow: 0 8px 24px rgba(0, 0, 0, 0.12);
    text-align: left;
  }
  .m-stealgarden__pop-img {
    display: block;
    border-radius: calc(var(--corner-radius, 6px) - 2px);
    overflow: hidden;
    margin-bottom: 4px;
  }
  .m-stealgarden__pop-img img {
    width: 100%;
    height: auto;
    vertical-align: middle;
    display: block;
  }
  .m-stealgarden__pop-title {
    font-size: 13px;
    font-weight: 500;
    line-height: 1.35;
    display: -webkit-box;
    -webkit-line-clamp: 2;
    -webkit-box-orient: vertical;
    overflow: hidden;
  }
  .m-stealgarden__pop-price {
    font-size: 12px;
    opacity: 0.85;
  }
  .m-stealgarden__pop--top {
    bottom: calc(50% + (var(--m-sg-pin-size) * 0.35));
    left: 50%;
    transform: translate(-50%, 6px);
  }
  .m-stealgarden__pop--bottom {
    top: calc(50% + (var(--m-sg-pin-size) * 0.35));
    left: 50%;
    transform: translate(-50%, -6px);
  }
  @media (hover: hover) and (pointer: fine) {
    .m-stealgarden__pin:hover .m-stealgarden__pop,
    .m-stealgarden__pin:focus-visible .m-stealgarden__pop {
      opacity: 1;
      visibility: visible;
    }
    .m-stealgarden__pin:hover .m-stealgarden__pop--top,
    .m-stealgarden__pin:focus-visible .m-stealgarden__pop--top {
      transform: translate(-50%, 0);
    }
    .m-stealgarden__pin:hover .m-stealgarden__pop--bottom,
    .m-stealgarden__pin:focus-visible .m-stealgarden__pop--bottom {
      transform: translate(-50%, 0);
    }
  }
  .m-stealgarden__pin:hover,
  .m-stealgarden__pin:focus {
    z-index: 8;
  }
{% endstyle %}

{% schema %}
{
  "name": "Steal the garden",
  "tag": "section",
  "class": "m-section-steal-garden",
  "disabled_on": {
    "groups": ["header", "footer", "aside"]
  },
  "settings": [
    {
      "type": "header",
      "content": "Content"
    },
    {
      "type": "color_scheme",
      "id": "color_scheme",
      "label": "Color scheme",
      "default": "scheme_1"
    },
    {
      "type": "image_picker",
      "id": "image",
      "label": "Scene image"
    },
    {
      "type": "select",
      "id": "image_aspect_ratio",
      "label": "Image shape",
      "default": "16/9",
      "options": [
        { "value": "original", "label": "Use image (when image is set)" },
        { "value": "16/9", "label": "Wide (16:9)" },
        { "value": "4/3", "label": "4:3" },
        { "value": "3/4", "label": "3:4 portrait" },
        { "value": "1/1", "label": "Square" }
      ]
    },
    {
      "type": "range",
      "id": "image_radius",
      "label": "Image corner radius",
      "min": 0,
      "max": 48,
      "step": 2,
      "unit": "px",
      "default": 16
    },
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "Steal This Garden"
    },
    {
      "type": "select",
      "id": "heading_tag",
      "label": "Heading level",
      "default": "h2",
      "options": [
        { "value": "h1", "label": "H1" },
        { "value": "h2", "label": "H2" },
        { "value": "h3", "label": "H3" }
      ]
    },
    {
      "type": "textarea",
      "id": "subtitle",
      "label": "Subtitle",
      "default": "Click the dots to shop every plant in this scene"
    },
    {
      "type": "range",
      "id": "title_size_mobile",
      "label": "Heading size (mobile)",
      "min": 20,
      "max": 44,
      "step": 1,
      "unit": "px",
      "default": 28
    },
    {
      "type": "range",
      "id": "title_size_desktop",
      "label": "Heading size (desktop)",
      "min": 24,
      "max": 56,
      "step": 1,
      "unit": "px",
      "default": 40
    },
    {
      "type": "range",
      "id": "subtitle_size",
      "label": "Subtitle size",
      "min": 12,
      "max": 20,
      "step": 1,
      "unit": "px",
      "default": 15
    },
    {
      "type": "range",
      "id": "header_gap",
      "label": "Space below header",
      "min": 8,
      "max": 48,
      "step": 2,
      "unit": "px",
      "default": 24
    },
    {
      "type": "header",
      "content": "Hotspots"
    },
    {
      "type": "select",
      "id": "hotspot_style",
      "label": "Dot style (preset)",
      "default": "light",
      "info": "Base look when custom colors below are not set.",
      "options": [
        { "value": "light", "label": "Light (white dot)" },
        { "value": "dark", "label": "Dark" }
      ]
    },
    {
      "type": "color",
      "id": "hotspot_fill_color",
      "label": "Dot fill color",
      "info": "Optional. Overrides preset center color."
    },
    {
      "type": "color",
      "id": "hotspot_border_color",
      "label": "Dot border color",
      "info": "Ring around the center dot (outline)."
    },
    {
      "type": "color",
      "id": "hotspot_pulse_color",
      "label": "Pulse ring fill",
      "info": "Outer animated halo."
    },
    {
      "type": "color",
      "id": "hotspot_pulse_border_color",
      "label": "Pulse ring border",
      "info": "Thin outline on the pulse halo."
    },
    {
      "type": "color",
      "id": "hotspot_arrow_color",
      "label": "“Click here” arrow color",
      "info": "Leave blank to use white on dark scenes / preset."
    },
    {
      "type": "checkbox",
      "id": "show_hotspot_arrow",
      "label": "Show “click here” arrow",
      "default": true
    },
    {
      "type": "select",
      "id": "hotspot_dot_animation",
      "label": "Dot animation",
      "default": "pulse",
      "options": [
        { "value": "pulse", "label": "Pulse (expand)" },
        { "value": "bounce", "label": "Bounce" },
        { "value": "breathe", "label": "Breathe (soft)" },
        { "value": "none", "label": "None" }
      ]
    },
    {
      "type": "range",
      "id": "hotspot_size",
      "label": "Dot tap target",
      "min": 36,
      "max": 56,
      "step": 2,
      "unit": "px",
      "default": 44
    },
    {
      "type": "checkbox",
      "id": "show_preview",
      "label": "Show product preview on hover (desktop)",
      "default": true
    },
    {
      "type": "header",
      "content": "Scroll reveal"
    },
    {
      "type": "select",
      "id": "animations",
      "label": "Animation",
      "default": "inherit",
      "info": "Uses Theme settings → Animations when Inherit.",
      "options": [
        { "value": "inherit", "label": "Inherit global setting" },
        { "value": "none", "label": "None" },
        { "value": "fade-in", "label": "Fade in" },
        { "value": "fade-in-up", "label": "Fade in up" },
        { "value": "fade-in-left", "label": "Fade in left" },
        { "value": "fade-in-right", "label": "Fade in right" }
      ]
    },
    {
      "type": "header",
      "content": "Section padding"
    },
    {
      "type": "range",
      "id": "padding_top_mobile",
      "label": "Padding top (mobile)",
      "min": 0,
      "max": 100,
      "step": 2,
      "unit": "px",
      "default": 36
    },
    {
      "type": "range",
      "id": "padding_bottom_mobile",
      "label": "Padding bottom (mobile)",
      "min": 0,
      "max": 100,
      "step": 2,
      "unit": "px",
      "default": 36
    },
    {
      "type": "range",
      "id": "padding_top",
      "label": "Padding top (desktop)",
      "min": 0,
      "max": 120,
      "step": 2,
      "unit": "px",
      "default": 52
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "label": "Padding bottom (desktop)",
      "min": 0,
      "max": 120,
      "step": 2,
      "unit": "px",
      "default": 52
    }
  ],
  "blocks": [
    {
      "type": "hotspot",
      "name": "Product dot",
      "settings": [
        {
          "type": "product",
          "id": "product",
          "label": "Product"
        },
        {
          "type": "paragraph",
          "content": "Dots use percent of the scene (0 = top/left, 100 = bottom/right). Move each one with the Vertical and Horizontal sliders. In the theme editor, draft dots (orange ring) show until you pick a product."
        },
        {
          "type": "range",
          "id": "offset_top",
          "label": "Vertical position",
          "min": 0,
          "max": 100,
          "step": 1,
          "unit": "%",
          "default": 50
        },
        {
          "type": "range",
          "id": "offset_left",
          "label": "Horizontal position",
          "min": 0,
          "max": 100,
          "step": 1,
          "unit": "%",
          "default": 50
        }
      ]
    }
  ],
  "max_blocks": 20,
  "presets": [
    {
      "name": "Steal the garden",
      "blocks": [
        {
          "type": "hotspot",
          "settings": {
            "offset_top": 42,
            "offset_left": 18
          }
        },
        {
          "type": "hotspot",
          "settings": {
            "offset_top": 38,
            "offset_left": 45
          }
        },
        {
          "type": "hotspot",
          "settings": {
            "offset_top": 55,
            "offset_left": 72
          }
        },
        {
          "type": "hotspot",
          "settings": {
            "offset_top": 48,
            "offset_left": 88
          }
        }
      ]
    }
  ]
}
{% endschema %}
